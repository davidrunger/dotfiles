#!/usr/bin/env bash

# Prints the git branches, except:
# - the repo's main branch
# - the current branch

git for-each-ref --format="%(refname:short)" refs/heads | \
  awk '!($0 == main || $0 == branch)' main="$(main-branch)" branch="$(branch)"
