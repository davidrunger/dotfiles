#!/usr/bin/env bash

# roll back Rails database migrations

# Remove stale fixtures so specs regenerate them for the current branch's schema.
rm spec/fixtures/*.yml

bin/rails db:rollback db:test:prepare
