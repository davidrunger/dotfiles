#!/usr/bin/env bash

# run Rails database migrations

# Remove stale fixtures so specs regenerate them for the current branch's schema.
rm -f spec/fixtures/*.yml

DISABLE_TYPELIZER=1 bin/rails db:migrate db:test:prepare
