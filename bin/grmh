#!/usr/bin/env bash

# [g]it [r]evert, accepting the default commit [m]essage provided by git (revert [h]ead)

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

grm HEAD
