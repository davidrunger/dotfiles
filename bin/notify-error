#!/usr/bin/env bash

# Creates a system notification with an error icon and a display duration of 1 hour.

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

notify "$1" "$2" --icon=error --time=3600
