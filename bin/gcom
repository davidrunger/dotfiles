#!/usr/bin/env bash

# [g]it [com]mit the staged changes (writing message in editor)

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

verify-on-ok-branch && git commit --verbose
