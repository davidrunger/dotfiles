#!/usr/bin/env bash

# [g]it [r]evert (accepting the default commit message provided by git)

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

git revert --no-edit "$@"
