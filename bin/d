#!/usr/bin/env bash

# Clear screen, including scrollback history.

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

# NOTE: I have only tested this on Linux.
tput clear
