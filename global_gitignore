# OS generated files #
######################
.DS_Store
.Trashes

# General stuff to ignore in repos
.env
.env.development.local
.env.test.local
/node_modules/

# fcom
.fcom.yml

# Personal stuff and customization
/personal/
config/initializers/z.rb
spec/.rubocop.yml
app/workers/load_runner.rb

# binstubs missing from commonlit repo
bin/erblint
bin/i18n-tasks
bin/rubocop

# Executing TypeScript in node
# we need this to be in `app/javascript/` (not `personal/`) to use the project TypeScript config
# We can't have a file called `typescript.ts`! https://commonlit.slack.com/archives/C5BFTS7NC/p1706036960165969?thread_ts=1706023596.962889&cid=C5BFTS7NC
app/javascript/typescript-scratchpad.ts
app/javascript/typescript-scratchpad.tsx
# we need this config for `ts-node` to work correctly (https://stackoverflow.com/a/72507086/4009384)
node.tsconfig.json
