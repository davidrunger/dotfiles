#!/usr/bin/env bash

set -euo pipefail # exit on any error, don't allow undefined variables, pipes don't swallow errors

lint secrets

background-and-notify newlines
background-and-notify prettier
background-and-notify rubocop
background-and-notify shellcheck
